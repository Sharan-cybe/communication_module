1.Activate the environment
2.Run the backend with uvicorn app.main:app --reload --port 8000
3.Run the ngrok terminal with 8000 port
4.To run the frontend u need to move in to the directory with cd Frontend and then run vercel --prod after that frontend is deployed
